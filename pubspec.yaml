name: loginn3
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.4 <3.6.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase Core - Inicialização do Firebase
  firebase_core: ^3.8.0

  # Firebase Authentication - Para login e autenticação de usuários
  firebase_auth: ^5.3.3

  # Google Sign-In - Integração com login do Google
  google_sign_in: ^6.2.0

  # Cloud Firestore - Banco de dados
  cloud_firestore: ^5.5.0

  # Ícones Cupertino
  cupertino_icons: ^1.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Lints para boas práticas
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
